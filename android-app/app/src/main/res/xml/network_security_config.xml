<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug/local-dev only: permits plain HTTP to the emulator's host-loopback
  alias (10.0.2.2 -> the developer machine's localhost) so the app can reach
  the API running in local Docker without HTTPS. Remove this once the API is
  deployed behind real TLS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
